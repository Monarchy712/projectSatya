# Satya UI Integration Guide (AI-Ready)

This directory contains customized, premium UI components designed to improve the **Infrastructure Governance Platform's** user experience. These changes focus on clarity, aesthetics, and contextual feedback.

## 📁 Changes to Apply

| File Name | Target Directory | Description |
| :--- | :--- | :--- |
| `AdminDashboard.jsx` | `frontend/src/components/Admin/` | Replaces full hashes with hoverable 'i' buttons. |
| `AdminDashboard.css` | `frontend/src/components/Admin/` | Styles for 'i' buttons and tooltips. |
| `SignatoryDashboard.jsx` | `frontend/src/components/Admin/` | Replaces hashes with hoverable 'i' buttons (consistency). |
| `LoadingOverlay.jsx` | `frontend/src/components/UI/` | Adds "Folder Transfer" animation logic for backend states. |
| `LoadingOverlay.css` | `frontend/src/components/UI/` | CSS for folder shape and falling file particles. |

## 🛠 Integration Steps for AI

To integrate these changes, follow these precise steps to avoid merge conflicts or breaking existing features:

### 1. Hash Display Modification
Replace the standard `tenderAddress` strings in the dashboard cards with the **Infographic Pattern**:
- **Pattern**: Wrap the 'i' button in a container `.admin-tender-card__asset-info`.
- **Hover Logic**: Ensure the component includes a tooltip span as a child of the info button.
- **Reference**: Use the `changes/AdminDashboard.jsx` as the master template.

### 2. Loading Animation Upgrade
The `LoadingOverlay` now supports a **Folder Transfer Animation**. This is triggered by specific context flags:
- **Trigger**: When `props.context` is set to `'admin'` or `'backend'`, skip the default SVG spinner.
- **Render**: Inject the `FolderTransferAnimation` sub-component into the `.lo-animation-area` or `.lo-spinner-box`.
- **Styling**: Ensure `LoadingOverlay.css` is updated to include the `@keyframes fileDrop` and `.lo-folder` classes.

### 3. Style Reconciliation
- Ensure all components use the project's CSS variables (`--pink-600`, `--cream`, `--font-mono`, etc.) defined in `index.css`.
- The 'i' button tooltip should be positioned relatively to its container with high `z-index`.

## ⚠️ Important Considerations
- **No Dependencies Added**: These changes rely solely on Vanilla CSS and React. Do not run `npm install` for these updates.
- **No Copy Button**: The user explicitly requested **no copy button** for the hashes. Ensure you don't re-add `navigator.clipboard` functionality to these elements.
- **Context Awareness**: The folder animation is designed for **data transitions**. For authentication or generic tasks, keep using the standard circular spinner.

---
*Generated by Antigravity AI for seamless full-stack governance portal development.*
